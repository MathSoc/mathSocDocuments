#import "../template.typ": project;
#show: project.with(
  title: "MathSoc", 
  authors: "The Mathematics Society", 
  header-footer-enable: 1,
  header: [Bylaws],
)

#import "../cover-page.typ": cover-page;
#cover-page(title: "Bylaws");

= Hello

This is a redraft of MathSoc's constitution.

This is a display of what the header looks like. the header is automatically placed on all pages except the first.

Fonts work because I added them.
_Italic font_
